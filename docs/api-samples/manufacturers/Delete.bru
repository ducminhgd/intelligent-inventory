meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{API_BASE_URL}}/manufacturers/{{MANUFACTURER_ID}}
  body: json
  auth: inherit
}

body:json {
  {
    "deleted_by": "{{$randomUUID}}"
  }
}
